# ── requirements.txt ──────────────────────────────────────────────────────
# Install with:  pip install -r requirements.txt

Django>=5.0,<5.3
djangorestframework>=3.15
django-cors-headers>=4.3
python-decouple>=3.8        # reads .env file into settings